const people = ['Ahmed','Ihab','Yousry'];
const ages = [20,60,80];
console.log(people);

module.exports = {
    people , ages     //this is instead of people : people   we do that if they have the same name
};